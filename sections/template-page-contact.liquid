<div class="container mb-5">
    <div class="row">
        <div class="col-12">
            <h1 class="text-center">{{ page.title }}</h1>
        </div>
    </div>

    <div class="row">
        {% form 'contact' %}
            {% if form.posted_successfully? %}
                <div class="rounded-0 mt-2 alert alert-success">
                    Grazie per averci contattati
                </div>
            {% endif %}

            {% if form.errors %}
                <div class="errorForm feedback">
                    <p>Ci dispiace, il tuo messaggio non è stato inviato perché conteneva
                        {{ form.errors | size | pluralize: 'un errore', 'alcuni errori' }}.<br/>Correggi i seguenti errori e invia di nuovo:</p>
                    {% for field in form.errors %}
                        <p>
                            <strong>Il campo
                                {{ field | capitalize | replace: 'Body', 'Comments / questions' }}

                                {{ form.errors.messages[field] }}.</strong>
                        </p>
                    {% endfor %}
                </div>
            {% endif %}

            <div class="row">
                <div class="col-12 col-md-6">
                    <label for="ContactFormName">Nome</label>
                    <input class="form-control rounded-0" id="ContactFormName" name="contact[first_name]" type="text">
                </div>
                <div class="col-12 col-md-6">
                    <label for="ContactFormName">Cognome</label>
                    <input class="form-control rounded-0" id="ContactFormName" name="contact[last_name]" type="text">
                </div>

            </div>
            <div class="col-12">
                <label for="ContactFormEmail">Email</label>
                <input class="form-control rounded-0" id="ContactFormEmail" name="contact[email]" oninput="setCustomValidity('')" oninvalid="this.setCustomValidity('Includi una email valida')" required type="email">
            </div>

            <div class="col-12">
                <label for="ContactOrderNumber">Numero dell'ordine #</label>
                <input class="form-control rounded-0" id="ContactFormOrderNumber" name="contact[order_number]" type="text">
            </div>

            <div class="col-12">
                <label for="ContactFormPhone">Numero di telefono</label>
                <input class="form-control rounded-0" id="ContactFormPhone" name="contact[phone]" type="tel">
            </div>

            <div class="col-12">
                <label for="ContactFormMessage">Messaggio</label>
                <textarea class="form-control rounded-0" id="ContactFormMessage" name="contact[body]" oninput="setCustomValidity('')" oninvalid="this.setCustomValidity('Scrivi un messaggio')" required rows="10"></textarea>
            </div>


            <input class="btn btn-dark w-100 mt-2 rounded-0" type="submit" value="Invia">
        {% endform %}
    </div>
</div> 